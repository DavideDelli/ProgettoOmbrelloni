// Questo file non è più necessario per la visualizzazione della mappa,
// poiché ora tutti gli ombrelloni sono mostrati direttamente.
// Può essere rimosso o lasciato vuoto.

document.addEventListener('DOMContentLoaded', function() {
    // Eventuali script futuri possono essere inseriti qui.
});
